{
  "name": "class-room-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix backend && npm run build --prefix backend",
    "start": "node backend/dist/server.js"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
